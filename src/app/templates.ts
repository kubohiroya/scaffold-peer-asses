// import { getSelectedCourseworks } from "./classroom/selectors";
// import { createOrSelectSheetBySheetName } from "./sheetUtil";

export function createConfigTemplate() {
  /*
  const {
    courseId,
    courseName,
    courseWorkId,
    courseWorkName,
  } = getSelectedCourseworks();
  const sheet = createOrSelectSheetBySheetName(
    `config:${courseName} ${courseWorkName}`
  );
   */
}
// export function createMapTemplate() {}
// export function createFormTemplate() {}
